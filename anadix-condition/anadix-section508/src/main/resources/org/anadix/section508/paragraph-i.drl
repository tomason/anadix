#created on: 9.6.2011
package org.analyzer.section508

import org.analyzer.html.*
imporg.anadix.section508.ReportFactorytory

rule "frame without title"
	when
		frame : FrameTag( title == null || == "" )
	then 
		insert(ReportFactory.newTitleMissing(frame));
end
